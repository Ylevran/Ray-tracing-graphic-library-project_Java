package elements;

import primitives.Color;
import primitives.Point3D;
import primitives.Vector;

public class SpotLight extends Light {
    public SpotLight(Color color, Point3D point3D, Vector vector, int i, double v, double v1) {
        super(color);
    }
}
