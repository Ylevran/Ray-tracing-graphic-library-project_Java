package elements;

import primitives.Color;
import primitives.Point3D;

public class PointLight extends Light {
    public PointLight(Color color, Point3D point3D, int i, double v, double v1) {
        super(color);
    }
}
